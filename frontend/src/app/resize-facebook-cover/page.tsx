import { createResizeToolPage } from "@/lib/pageFactories/resizeToolPageFactory";

const { metadata, Page } = createResizeToolPage("resize-facebook-cover");

export { metadata };
export default Page;
