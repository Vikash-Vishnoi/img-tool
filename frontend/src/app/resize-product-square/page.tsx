import { createResizeToolPage } from "@/lib/pageFactories/resizeToolPageFactory";

const { metadata, Page } = createResizeToolPage("resize-product-square");

export { metadata };
export default Page;
