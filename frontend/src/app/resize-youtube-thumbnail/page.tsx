import { createResizeToolPage } from "@/lib/pageFactories/resizeToolPageFactory";

const { metadata, Page } = createResizeToolPage("resize-youtube-thumbnail");

export { metadata };
export default Page;
